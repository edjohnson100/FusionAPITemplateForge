# CLAUDE.md

This is a Fusion 360 script (`{{NAME}}.py`), generated by Fusion API Template Forge.

There is no build system, package manager, or test suite -- this only runs inside Fusion 360's embedded Python interpreter. `adsk.core`/`adsk.fusion` only exist inside the Fusion 360 process, so there is no headless way to run or test this code.

To verify a change: Fusion 360 -> Utilities -> Add-Ins -> Scripts and Add-Ins -> Scripts tab -> Run, then exercise the change interactively. Use `app.log(...)` (see the existing `except` block in `{{NAME}}.py`) to write to the Text Command window for debugging.
