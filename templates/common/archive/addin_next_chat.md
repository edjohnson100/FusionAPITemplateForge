# Next Chat

Notes to pick a future session back up quickly -- current state, what's next, anything in progress or blocked. Rewrite this (don't just append) whenever the current state changes meaningfully, so it always reflects where things actually stand rather than a history of where they've been.

## Current state
Freshly generated by Fusion API Template Forge -- the rename/GUID/manifest work is already done, not yet built out.

## Next up
<!-- FORGE:IF palette -->
- Replace `commands/PaletteCommand.py`'s `DEFAULT_MAIN`/`validate_main` with the real feature's fields.
<!-- FORGE:IF previewGenerate -->
- Replace `commands/generation.py`'s `create_and_build()` stub with the real geometry construction.
<!-- FORGE:ENDIF -->
<!-- FORGE:ELSE -->
- Replace `commands/ExampleCommand.py`'s `command_execute()` stub with the real behavior.
<!-- FORGE:ENDIF -->
