# Dev Notes

A running, session-by-session log of notable feature work and design decisions for this add-in. Not auto-generated -- update it by hand when you finish a notable change, in roughly this style:

## YYYY-MM-DD -- Short title
What changed and why. Call out anything a future session (human or AI) would otherwise have to rediscover the hard way: a bug that shipped and its fix, an API limitation you worked around, a decision you made that looks wrong at a glance but isn't.
