# Next Steps

Your `{{NAME}}` script is ready to go -- the manifest ID, description, author, and version are already filled in.

1. Move (or keep) the `{{NAME}}` folder wherever you like. Fusion's default Scripts directory is:
   * **Windows:** `%appdata%\Autodesk\Autodesk Fusion 360\API\Scripts`
   * **Mac:** `~/Library/Application Support/Autodesk/Autodesk Fusion 360/API/Scripts`
2. In Fusion, press `Shift + S` to open the **Scripts and Add-Ins** dialog.
3. If you didn't use the default directory, click the **"+"** icon next to the search box and select **Script or add-in from device** to add the `{{NAME}}` folder.
4. Make sure the **Scripts** filter checkbox is checked, select `{{NAME}}`, and click **Run**.

That's it -- no manual manifest editing required.
