# Next Steps

Your `{{NAME}}` script is ready to go -- the manifest ID, description, author, and version are already filled in.

1. Open Fusion 360 -> Utilities -> Add-Ins -> Scripts and Add-Ins.
2. On the **Scripts** tab, click the green **+** and select this folder.
3. Select `{{NAME}}` and click **Run**.

That's it -- no manual manifest editing required.
